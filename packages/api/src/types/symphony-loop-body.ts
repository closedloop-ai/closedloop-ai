// Backward-compatibility shim — re-exports from the renamed module.
// Remove once all dependent repos have migrated to the new import path.
export type { LoopBody as SymphonyLoopBody } from "./loop-body";
