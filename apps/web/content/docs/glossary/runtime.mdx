---
title: Runtime
description: The environment that executes work and determines what tools or providers are available.
---

The runtime is where configured capabilities become real execution. In ClosedLoop.ai, the runtime is a registered compute target plus the tools on that machine plus the resolved Claude Code plugins plus the sandbox the gateway enforces.

See [Runtime (mechanisms)](../mechanisms/runtime).
