{
  "name": "@repo/mcp",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "tsx watch --require ./src/preload.ts --env-file=.env.local src/index.ts",
    "build": "tsc",
    "start": "tsx dist/apps/mcp/src/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.0",
    "@repo/api": "workspace:*",
    "@repo/database": "workspace:*",
    "tsx": "^4.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
