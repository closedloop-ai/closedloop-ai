# PLN-535 / PRD-258 System Health Check: Plugin Updates

## Inputs

- Repo: `symphony-alpha-prd-258`
- Work item: PRD-258
- Plan sources:
  - ClosedLoop MCP `PLN-535`, latestVersion 3, status APPROVED, id `019e13fe-5c78-730d-bcae-d9ebe1d2529d`
  - [/Users/daniel.ochoa/Source/.goal-plans/find-all-of-my-assigned-features-019c963c-019e0805/PRD-258-implementation-plan.md](/Users/daniel.ochoa/Source/.goal-plans/find-all-of-my-assigned-features-019c963c-019e0805/PRD-258-implementation-plan.md)
- Current code sources:
  - [packages/api/src/types/compute-target.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/packages/api/src/types/compute-target.ts:18)
  - [apps/api/app/compute-targets/validators.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/api/app/compute-targets/validators.ts:62)
  - [apps/api/app/compute-targets/[id]/health-check/route.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/api/app/compute-targets/[id]/health-check/route.ts:1)
  - [apps/api/app/compute-targets/service.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/api/app/compute-targets/service.ts:71)
  - [apps/app/lib/engineer/queries/keys.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/lib/engineer/queries/keys.ts:82)
  - [apps/app/lib/engineer/queries/health-check.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/lib/engineer/queries/health-check.ts:1)
  - [apps/app/lib/system-check/health-check-freshness.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/lib/system-check/health-check-freshness.ts:1)
  - [apps/app/lib/system-check/pre-loop-health-check.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/lib/system-check/pre-loop-health-check.ts:1)
  - [apps/app/lib/system-check/pre-loop-system-check-provider.tsx](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/lib/system-check/pre-loop-system-check-provider.tsx:1)
  - [apps/app/app/(authenticated)/components/system-check-bootstrap.tsx](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/app/(authenticated)/components/system-check-bootstrap.tsx:1)
  - [apps/app/components/engineer/HealthCheckDialog.tsx](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/components/engineer/HealthCheckDialog.tsx:1)
  - [apps/app/components/system-check/system-check-results.tsx](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/components/system-check/system-check-results.tsx:1)
  - [apps/app/hooks/queries/use-compute-targets.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/hooks/queries/use-compute-targets.ts:1)
  - [packages/observability/telemetry/schema.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/packages/observability/telemetry/schema.ts:1)
  - [apps/api/lib/desktop-telemetry-handler.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/api/lib/desktop-telemetry-handler.ts:1)
- Repo guardrails:
  - Additive cross-repo fields, missing values degrade safely, optional absent fields stay omitted.
  - `apps/app` must not call database directly; API routes stay thin and service-owned.
  - `just build` is required because schema/app/API changes touch the build graph.

## Behavior Areas

- Health-check request identity and owner-only mutation gate: user-visible mutation must be controlled by `plugin-auto-update`, target ownership, query key, URL, timeout, in-memory cache, and persisted snapshot mode.
- Snapshot persistence and API validation: Desktop metadata and request mode must survive validator, service, database, and API read/write boundaries.
- System Check rendering and analytics: structured remediation links and gated update status must render consistently in ambient and blocking dialogs without leaking enabled metadata into disabled surfaces.
- Desktop telemetry ingestion: new Desktop plugin-update telemetry must parse, sanitize, and reach Datadog-bound logs.

## Behavioral Edge-Case Expansion

- Structured-result contracts: rows S1-S7 cover URL/query construction, request mode default, validator parsing, and telemetry schema setup.
- Exported boundary invariants: rows S1-S7 require helper/service boundaries to enforce mode and optional-field defaults even when callers omit values.
- Contract-signal precedence: rows S1-S4 distinguish flag, owner/shared target, query param, cache key, snapshot field, and optional Desktop metadata; missing old fields default to disabled.
- Library-managed lifecycle re-entry: rows S1/S2/S4/S5 cover TanStack refetch, prefetch, persisted hydration, and recheck re-entry.
- Active-processing re-entry: existing pre-loop duplicate/cancel/recheck rows remain; intended rows preserve attempt scoping while adding mode.
- State propagation across isolation boundaries: rows S1-S4 cover browser -> Electron query, browser -> API snapshot, API -> DB, DB -> query cache.
- Cross-surface propagation and reconciliation: rows S2/S4 cover disabled/enabled cache mismatch, persisted snapshot bypass, and mode-matched hydration.
- Data visibility versus side effects: rows S1/S6 separate query/snapshot visibility from local plugin mutation and UI analytics side effects.
- Cached capability drift: not capability-gated; mode caches are covered by S2/S4.
- Terminal-state transition guards: no new terminal state; pre-loop cancel/resolve behavior stays existing and owner-scoped.
- Cancellation and shutdown: S1 covers 45s timeout only for enabled mode; old 5s remains disabled fallback.
- Finalizer-visible cleanup state: no new finalizer or cleanup handle in this repo.
- Time-bound credentials/signatures: no new credentials/signatures; relay command path remains existing `/api/gateway/*`.
- One-time side effects: plugin mutation is Electron-owned; this repo only decides whether to request it.
- Durable finalization and replay eligibility: no job finalization changes.
- Shared durable resources: no key/profile/resource deletion.
- Profile or config cloning: not applicable.
- Backward-compatible persisted defaults and promotion: rows S3-S4 cover old rows missing `pluginAutoUpdateEnabled` normalizing to false.
- Distributed lifecycle coverage: rows S1-S4 cover local, owner relay, shared relay, old Desktop, old snapshots, and repeated cache/hydration lifecycle.
- Diagnostic contracts: row S7 covers `diagnostics.pluginUpdate` exact category and bounded fields.
- Advertised status inventory: rows S5-S7 cover `success`, `failed`, `timeout`, `skipped`, UI event names, and telemetry categories.
- Exception scope: rows S3/S7 require schema parse errors to reject/drop invalid fields without collapsing unrelated payloads.
- Serverless async side effects: snapshot route awaits service write; no new fire-and-forget API side effect.
- Path and identity policy canonicalization: target ownership uses existing `ownerName` presence; route path remains gateway namespace.
- Transformed input validation parity: query param emitted as literal `pluginAutoUpdate=1`; absent/false/unknown omitted.
- Canonical value persistence: row S3 persists the boolean request mode separately from Desktop result JSON.
- Partial update preservation: row S3 requires result checks preserve new optional fields without dropping existing `mcpServers` or `debug`.
- Persistence access paths: new DB field is write/read metadata for latest snapshot identity; no index needed because existing lookup is by unique `computeTargetId`.
- Side-effect boundary: rows S1/S2 require disabled/shared/cache-hit paths to omit mutation query and avoid longer timeout.
- Testable invariant: Required Tests list positive and one-axis mutation coverage for every changed contract.

## Shared State Axes

- Feature flag state: enabled, disabled, unavailable/undefined.
- Target ownership: local gateway, owner relay, shared relay (`ownerName` present), no target/offline.
- Request mode: disabled/absent, enabled query param, old missing persisted field.
- Cache/snapshot mode: disabled entry, enabled entry, old missing mode, mode mismatch.
- Desktop payload shape: old no metadata, new metadata, unknown/invalid metadata.

### Health-Check Request Identity And Owner Gate

#### Current Code

Frozen pre-implementation baseline. Do not rewrite after implementation begins.

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| `healthCheckOptions` | target key, expected MCP URL, latest version | Query key excludes auto-update mode | Builds `/api/gateway/health-check` or relay URL without `pluginAutoUpdate` | Disabled and future enabled responses would share cache | [keys.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/lib/engineer/queries/keys.ts:82), [health-check.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/lib/engineer/queries/health-check.ts:193) |
| Pre-loop provider | selected relay target, latest version | Always uses 5s timeout | Fetches with current query key/URL; no owner flag decision | Synchronous Desktop update would time out; shared targets would be indistinguishable | [pre-loop-system-check-provider.tsx](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/lib/system-check/pre-loop-system-check-provider.tsx:615) |
| Pre-loop cache lookup | cached data, expected MCP URL, latest version | Freshness ignores auto-update mode | May reuse any fresh matching target/MCP/version data | Disabled data could satisfy enabled request and vice versa | [health-check-freshness.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/lib/system-check/health-check-freshness.ts:51) |
| Ambient bootstrap | local/relay routing | Prefetches and hydrates by target/version only | No feature flag/owner gate; no mode in snapshot freshness | Ambient flow cannot opt into plugin update safely | [system-check-bootstrap.tsx](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/app/(authenticated)/components/system-check-bootstrap.tsx:45) |
| Settings/chat drawers | manual settings or MCP availability check | Uses default health-check query | Non-mutating today | Must remain disabled after feature lands | [local-compute-targets-card.tsx](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/app/(authenticated)/settings/components/local-compute-targets-card.tsx:229), [DocumentChatDrawer.tsx](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/components/chat/DocumentChatDrawer.tsx:45) |

#### Intended Change

Frozen target behavior derived from PLN-535. Only change after implementation if recording a plan clarification.

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| S1 `healthCheckOptions` | `pluginAutoUpdateEnabled` boolean | Include mode in query key and append `pluginAutoUpdate=1` only when true | Local URL and relay URL carry opt-in; false/undefined omits it | Old clients and disabled paths remain non-mutating | PLN-535 AC-001, AC-014 |
| S2 Pre-loop provider | `plugin-auto-update` flag and selected target owner | `flag === true && ownerName absent` enables mode; shared/off/unavailable disables | Enabled owner/local uses 45s timeout; disabled/shared uses 5s and disabled cache key | Owner-only mutation; shared relay cannot mutate another machine | PLN-535 AC-008, AC-017 |
| S3 Pre-loop cache | cache entry mode and requested mode | Fresh only when expected MCP URL, latest version, and mode all match | Old missing mode normalizes to false | Disabled and enabled responses cannot satisfy each other | PLN-535 AC-014 |
| S4 Ambient bootstrap | local/owner relay/shared relay, flag state, snapshot mode | Local and owner relay may enable; shared relay disabled | Hydrates only mode-matched persisted snapshots; off-to-on bypasses old false snapshots | Ambient dialog uses same mode-safe identity as blocking dialog | PLN-535 AC-009, AC-014 |
| S5 Settings/chat drawers | manual Settings or MCP-only checks | Pass or rely on explicit false | No `pluginAutoUpdate=1`; disabled key hides gated metadata | Non-mutating auxiliary checks stay stable | PLN-535 T-3.12, T-3.13 |

### Snapshot Persistence And API Validation

#### Current Code

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| API `PUT /compute-targets/:id/health-check` | request body | Validator allows only current check fields and no mode | New row metadata is stripped; no mode column exists | Persisted relay snapshot loses update metadata and cannot separate enabled/disabled | [validators.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/api/app/compute-targets/validators.ts:62), [schema.prisma](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/packages/database/prisma/schema.prisma:431) |
| Service upsert/read | accessible target and payload | Stores result JSON, expected MCP URL, latest version | Returns snapshot without request mode | Old and new readers cannot tell mutation mode | [service.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/api/app/compute-targets/service.ts:98), [service.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/api/app/compute-targets/service.ts:1123) |

#### Intended Change

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| S6 API validator | Desktop `checks[]` row with optional update fields | Accept boolean `updateAttempted`, enum `updateOutcome`, string array `updatePluginIds`, structured `remediationLinks` | Preserve fields through parse/storage/read; unknown extra fields stripped/passthrough per existing style | New Desktop metadata survives; old Desktop without fields still parses | PLN-535 AC-015, AC-016 |
| S7 Service/DB | optional `pluginAutoUpdateEnabled` input | Persist boolean column with default false; normalize omitted input/old row to false | Upsert and read include mode; no speculative index | Snapshot identity is durable outside result JSON | PLN-535 AC-015 |

### System Check Rendering And Analytics

#### Current Code

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| `SystemCheckResults` | check error/remediation | Renders remediation as monospaced text only | No structured links, no URL tokenization | Docs URL is not clickable/accessibility friendly | [system-check-results.tsx](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/components/system-check/system-check-results.tsx:204) |
| `HealthCheckDialog` | dialog mode/data | Passes renderable checks only | Does not pass auto-update mode to query or results | Update metadata cannot be gated by mode | [HealthCheckDialog.tsx](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/app/components/engineer/HealthCheckDialog.tsx:112) |

#### Intended Change

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| S8 `HealthCheckDialog` | explicit `pluginAutoUpdateEnabled` prop | Mirrors caller mode; does not evaluate feature flag itself | Query, recheck, and `SystemCheckResults` use same mode | Blocking and ambient dialogs render consistently | PLN-535 AC-009 |
| S9 `SystemCheckResults` | plugin row metadata and mode | If mode true, show `Updated`, `Update failed`, or `Update timed out`; if false, hide gated metadata | Pass/fail icon semantics unchanged | Enabled metadata visible only to enabled surfaces | PLN-535 AC-003, AC-014 |
| S10 Remediation links | structured links or legacy remediation text | Render structured links first as anchors; tokenize `https://` fallback safely | Analytics fires viewed/clicked only for visible structured block | Accessible docs link and bounded analytics | PLN-535 AC-010, AC-016 |

### Desktop Telemetry Ingestion

#### Current Code

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| `desktopTelemetryEventSchema` | Desktop telemetry event | Known diagnostics schema has no `pluginUpdate` | Unknown diagnostics stripped by schema | Plugin-update diagnostics would be lost | [schema.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/packages/observability/telemetry/schema.ts:376) |
| `handleTelemetryEvent` | parsed event | Sanitizes and logs accepted diagnostics | No plugin-update-specific handling | Datadog metadata cannot query update diagnostics | [desktop-telemetry-handler.ts](/Users/daniel.ochoa/Source/symphony-alpha-prd-258/apps/api/lib/desktop-telemetry-handler.ts:104) |

#### Intended Change

| Entry Path | State Inputs | Decision / Branch | Actions / Side Effects | External Outcome | Source |
| --- | --- | --- | --- | --- | --- |
| S11 Telemetry schema | categories `plugin_update.*` and `diagnostics.pluginUpdate` | Accept exact bounded shape; strip unknown future fields; reject/drop invalid enums per schema style | Top-level `pluginUpdate` outside `diagnostics` not accepted | Desktop producer contract survives schema parse | PLN-535 AC-018 |
| S12 Telemetry handler | parsed plugin-update event | Sanitization preserves `diagnostics.pluginUpdate` | Logs Datadog-bound metadata with origin desktop | Queryable plugin update attempt/outcome/failure details | PLN-535 T-4.4 |

## Delta Checklist

- Add `pluginAutoUpdateEnabled` to health-check query key, request builder, pre-loop cache/freshness, ambient snapshot hydration, persisted snapshot types, service, DB schema, and migration.
- Evaluate `plugin-auto-update` through existing analytics/PostHog helpers in caller surfaces only; `HealthCheckDialog` mirrors an explicit prop.
- Preserve old Desktop response shapes and old snapshot inputs by defaulting all new fields to absent/false.
- Keep Settings, DocumentChatDrawer, and BranchChatDrawer non-mutating.
- Preserve `remediationLinks` through API validation and render structured links before legacy URL tokenization.
- Add `plugin_update.*` categories and `diagnostics.pluginUpdate` schema/handler coverage.
- Do not add new environment variables.

## Required Tests

- `health-check.test.ts`: enabled local and relay requests append `pluginAutoUpdate=1`; disabled/undefined omit; normalization preserves update fields and links.
- `health-check-freshness.test.ts`: mode mismatch is stale; old missing mode is disabled; URL/latest comparisons still apply.
- `pre-loop-health-check.test.ts`: timeout helper returns 45s only for enabled mode, 5s otherwise.
- `pre-loop-system-check-provider.test.tsx`: owner/local flag-enabled path enables mode and 45s timeout; flag off/unavailable/shared paths disable; cache mismatch fetches.
- `system-check-bootstrap.test.tsx`: ambient local/owner opt-in, shared suppression, persisted snapshot mode match/bypass.
- `HealthCheckDialog.test.tsx`: prop flows into `healthCheckOptions` and `SystemCheckResults`; dialog does not evaluate flag.
- `compute-targets-card.test.tsx`: Settings checks stay disabled and do not reuse enabled metadata.
- `system-check-results.test.tsx`: badges, hidden disabled metadata, structured links, fallback URL tokenization, old Desktop payloads.
- API/service tests: validator round-trip for update fields; `pluginAutoUpdateEnabled` storage, read, omitted old input default false.
- Telemetry schema/handler tests: exact `diagnostics.pluginUpdate` accepted, bounded, sanitized/logged, and invalid/top-level shapes rejected or stripped.

## Verification Findings

Pending implementation.

## Fixes Applied

Pending implementation.

## Final Alignment Status

Not aligned

Implementation and validation have not run yet.

## Post-Implementation Verification Findings

| Diagram / Plan Branch | Verification Evidence | Status |
| --- | --- | --- |
| Flag disabled or unavailable sends disabled request | `SystemCheckBootstrap` and `PreLoopSystemCheckProvider` require `NEXT_PUBLIC_POSTHOG_KEY` and `useFeatureFlag("plugin-auto-update").enabled === true`; disabled/fallback mode omits `pluginAutoUpdate=1`, uses the disabled query key, and keeps 5s pre-loop timeout. | Aligned |
| Shared relay target takes disabled branch | Owner mode is derived from missing `ownerName`; shared targets keep `pluginAutoUpdateEnabled=false` even when the flag is enabled. | Aligned |
| Owner/local enabled branch sends `pluginAutoUpdate=1` | `healthCheckOptions` carries explicit mode into query keys and request URLs; `buildHealthCheckRequest` appends `pluginAutoUpdate=1` only when mode is true. | Aligned |
| Optional metadata survives API validation and snapshot storage | `CheckResult` validators preserve `updateAttempted`, `updateOutcome`, `updatePluginIds`, and `remediationLinks`; snapshot service stores/returns `pluginAutoUpdateEnabled` in a dedicated DB column with default false. | Aligned |
| Enabled and disabled caches/snapshots do not cross-contaminate | Query keys and freshness entries include `pluginAutoUpdateEnabled`; old/missing snapshot mode normalizes to false, so enabled reads bypass disabled snapshots. | Aligned |
| UI renders update metadata only for enabled surfaces | `HealthCheckDialog` mirrors an explicit prop and `SystemCheckResults` hides update badges/structured links unless `pluginAutoUpdateEnabled` is true. | Aligned |
| Telemetry branch preserves Desktop diagnostics | `packages/observability` accepts bounded `plugin_update.*` categories and `diagnostics.pluginUpdate`; API telemetry handler preserves diagnostics under desktop origin metadata. | Aligned |
| Older-client branch remains additive | New request/response fields are optional/defaulted; old Desktop response shapes without update fields still parse and render base health-check rows. | Aligned |

## Post-Implementation Fixes Applied

- Fixed mode propagation and test mocks after the first app-focused validation pass exposed an undefined `pluginAutoUpdateEnabled` helper value and missing `useFeatureFlag` mocks.
- Built the local `@closedloop-ai/loops-api` package before focused app tests because this worktree lacked the generated `dist` subpath exports used by Vite.
- Fixed touched-file Ultracite errors in `HealthCheckDialog`, `SystemCheckResults`, `health-check-freshness`, and pre-loop tests/provider without touching unrelated lint warnings.

## Post-Implementation Validation

- `pnpm -C apps/app exec vitest run 'lib/engineer/queries/__tests__/health-check.test.ts' 'lib/system-check/__tests__/health-check-freshness.test.ts' 'lib/system-check/__tests__/pre-loop-health-check.test.ts' 'lib/system-check/__tests__/pre-loop-system-check-provider.test.tsx' 'components/system-check/__tests__/system-check-results.test.tsx' 'components/engineer/__tests__/HealthCheckDialog.test.tsx' 'app/(authenticated)/components/__tests__/system-check-bootstrap.test.tsx' 'app/(authenticated)/settings/components/__tests__/compute-targets-card.test.tsx'`: passed, 8 files / 91 tests.
- `pnpm -C apps/api exec vitest run '__tests__/api/compute-targets.test.ts' 'app/compute-targets/service.test.ts' '__tests__/desktop-telemetry-handler.test.ts'`: passed, 3 files / 74 tests.
- `pnpm -C packages/observability exec vitest run '__tests__/telemetry-schema.test.ts'`: passed, 1 file / 64 tests.
- `just db-generate`: passed.
- `pnpm -C apps/app typecheck`: passed.
- `pnpm -C apps/api typecheck`: passed.
- `pnpm lint`: passed with pre-existing warnings only.
- `git diff --check`: passed.
- `just build`: passed, 21 successful tasks.

## Post-Implementation Final Alignment Status

Aligned

The final code matches PLN-535 and PRD-258 for the symphony-alpha owned/local enabled path, disabled/fallback path, shared-target suppression path, additive API/snapshot compatibility, UI rendering, telemetry ingestion, and required validation.

## Fix Pass Verification Findings - 2026-05-11 Direct Relay Shared Target Guard

| Review Finding | Decision / Branch | Fix Evidence | Status |
| --- | --- | --- | --- |
| Direct relay calls could pass `pluginAutoUpdate=1` for shared targets because `ensureTargetOwnedAndOnline()` accepts accessible shared targets and `toGatewayPath()` preserved the original query string. | Strip rather than reject for shared health-check relay requests. This matches the PLN-535 shared-target disabled branch by preserving read-only/shared health-check access while removing the mutating auto-update request before Electron sees it. | `apps/app/app/api/gateway-relay/[...path]/route.ts` now removes `pluginAutoUpdate` from health-check gateway paths when the compute target has `ownerName`, and snapshot persistence reads auto-update mode from the sanitized gateway path. | Aligned |
| Owner/local enabled behavior must remain compatible. | Owned targets continue to forward the original `pluginAutoUpdate=1` health-check query. Missing or older-client query parameters continue to default to disabled. | `apps/app/app/api/gateway-relay/[...path]/__tests__/route.test.ts` covers shared stripping and owned preservation at the route boundary. | Aligned |

## Fix Pass Fixes Applied

- Added a server-side relay guard for the shared-target branch so direct `/api/gateway-relay/health-check?pluginAutoUpdate=1` misuse cannot forward the mutating parameter to Electron for another user's Desktop.
- Kept the shared branch as a sanitized health-check request instead of a rejected request to preserve existing shared-target compatibility and match the Mermaid disabled branch.
- Added route-level regression coverage for shared direct relay misuse and owned/local enabled relay forwarding.

## Fix Pass Validation

- `pnpm -C apps/app exec vitest run 'app/api/gateway-relay/[...path]/__tests__/route.test.ts'`: passed, 1 file / 2 tests.

## Fix Pass Final Alignment Status

Aligned

The direct relay server boundary now matches PLN-535 AC-017 and the shared-target disabled branch. Remaining validation is in progress.

## Fix Pass Final Validation - 2026-05-11

- `pnpm -C apps/app exec vitest run 'app/api/gateway-relay/[...path]/__tests__/route.test.ts'`: passed, 1 file / 2 tests.
- `pnpm -C apps/app typecheck`: passed.
- `pnpm lint`: passed with pre-existing warnings outside this fix.
- `git diff --check`: passed.
- `just build`: passed, 21 successful tasks.

## Fix Pass Completion Status - 2026-05-11

Aligned

The review finding is closed in symphony-alpha. No closedloop-electron code change was required because the vulnerability was the app relay boundary forwarding a mutating query parameter to an accessible shared target.

## Review Thread Fix Verification Findings - 2026-05-11 Empty Cache Auto-Update Mode Guard

| Review Finding | Decision / Branch | Fix Evidence | Status |
| --- | --- | --- | --- |
| `discussion_r3215827512`: an empty cached/persisted health-check response could return fresh before comparing `pluginAutoUpdateEnabled`, allowing a disabled-mode empty snapshot to satisfy a later enabled auto-update request. | Compare cached auto-update mode before any empty-check freshness return. Keep old snapshots compatible by treating absent mode metadata as disabled. | `apps/app/lib/system-check/health-check-freshness.ts` now evaluates `(entry.pluginAutoUpdateEnabled ?? false) !== pluginAutoUpdateEnabled` immediately after timestamp validation and before `checks.length === 0`. | Aligned |
| Matching empty disabled snapshots should keep the existing one-day empty-cache freshness behavior. | Only mode mismatches bypass the empty-cache fresh return; matching disabled mode still follows the default freshness window. | `apps/app/lib/system-check/__tests__/health-check-freshness.test.ts` covers empty disabled-vs-enabled as stale and empty disabled-vs-disabled as fresh. | Aligned |

## Review Thread Fixes Applied - 2026-05-11

- Moved the plugin auto-update mode comparison ahead of renderable-check derivation and the empty-cache freshness return.
- Added focused regression coverage for an empty disabled-mode snapshot queried by an enabled request, while preserving matching disabled empty-cache freshness.

## Review Thread Validation - 2026-05-11

- `pnpm -C apps/app exec vitest run 'lib/system-check/__tests__/health-check-freshness.test.ts'`: passed, 1 file / 6 tests.
- `pnpm -C apps/app typecheck`: passed.

## Review Thread Final Alignment Status - 2026-05-11

Aligned

The review-thread blocker is fixed. Empty cached health-check entries no longer bypass auto-update mode mismatch detection, and older snapshots with absent mode metadata continue to behave as disabled-mode entries.

## Review Thread Fix Verification Findings - 2026-05-11 Ownership, Gating, Analytics, And Test Fixtures

| Review Finding | Decision / Branch | Fix Evidence | Status |
| --- | --- | --- | --- |
| `apps/app/app/(authenticated)/components/system-check-bootstrap.tsx`: a loading relay target was `undefined`, so `relayTarget !== null && !relayTarget?.ownerName` treated it as owned and could briefly enable plugin auto-update. | Treat unknown/loading relay targets as not owned. Only local gateway or a loaded relay target without `ownerName` can enable plugin auto-update. | `SystemCheckBootstrap` now uses `Boolean(relayTarget) && !relayTarget?.ownerName`; `system-check-bootstrap.test.tsx` covers loading relay target disabled and loaded owned relay enabled. | Aligned |
| `apps/app/components/system-check/system-check-results.tsx`: structured remediation links and viewed analytics were visible/emitted even when plugin auto-update mode was disabled. | Gate structured plugin-update remediation links on the same explicit `pluginAutoUpdateEnabled` mode as update badges. Legacy text remediation remains visible. | `SystemCheckResults` now includes `pluginAutoUpdateEnabled` in `showStructuredLinks`; `system-check-results.test.tsx` asserts the structured link is absent when disabled and present when enabled. | Aligned |
| Structured-link analytics used mixed `plugin_update_*` and `plugin_autoupdate_*` prefixes. | Use the `plugin_autoupdate_*` prefix consistently for the paired remediation viewed/clicked events. | The viewed event is now `plugin_autoupdate_remediation_viewed`; the clicked event remains `plugin_autoupdate_docs_link_clicked`. | Aligned |
| `HealthCheckDialog` classified any relay with disabled auto-update as `shared_relay`, including owned relays when the feature flag is off. | Classify target kind from explicit ownership, not from mutation mode. Disabled owned relays should stay `owned_relay`; shared relays stay `shared_relay`. | `HealthCheckDialog` accepts `isOwnedTarget`; bootstrap and pre-loop provider pass ownership, and `HealthCheckDialog.test.tsx` covers disabled owned relay vs shared relay classification. | Aligned |
| Runtime cache parsing compared query-key index 4 with a raw `"plugin-auto-update"` string. | Centralize the query-key mode segment to keep cache readers and key builders in sync. | `HEALTH_CHECK_AUTO_UPDATE_QUERY_SEGMENT` and `HEALTH_CHECK_NO_AUTO_UPDATE_QUERY_SEGMENT` are exported from `keys.ts` and reused by app health-check and compute-target cache keys plus pre-loop cache parsing. | Aligned |
| API test gaps obscured regressions: telemetry test accessed `infoCall.meta` without a guard and a compute-target fixture had `allRequiredPassed: true` with a failed required check. | Make test failures clear and fixtures internally consistent. | Added `expect(infoCall).toBeDefined()` before reading metadata and changed the fixture to `allRequiredPassed: false`. | Aligned |

## Review Thread Fixes Applied - 2026-05-11

- Fixed the ambient relay ownership loading branch so undefined relay target data cannot request plugin auto-update.
- Added ownership propagation into `HealthCheckDialog` for correct owned/shared relay UX and telemetry classification independent of auto-update mode.
- Gated structured plugin-update remediation links and viewed analytics on enabled auto-update mode, and normalized remediation analytics naming to `plugin_autoupdate_*`.
- Replaced duplicated query-key mode string literals with exported constants.
- Tightened focused regression tests for loading relay ownership, owned/shared relay classification, disabled structured-link absence, telemetry assertion clarity, and compute-target fixture consistency.

## Review Thread Validation - 2026-05-11

- `pnpm -C apps/app exec vitest run 'app/(authenticated)/components/__tests__/system-check-bootstrap.test.tsx' 'components/system-check/__tests__/system-check-results.test.tsx' 'components/engineer/__tests__/HealthCheckDialog.test.tsx' 'lib/system-check/__tests__/pre-loop-system-check-provider.test.tsx' 'lib/engineer/queries/__tests__/health-check.test.ts'`: passed, 5 files / 71 tests.
- `pnpm -C apps/api test __tests__/desktop-telemetry-handler.test.ts __tests__/api/compute-targets.test.ts`: passed, 2 files / 49 tests.
- `pnpm -C apps/app typecheck`: passed.
- `pnpm lint`: passed with pre-existing warnings outside this fix.
- `git diff --check`: passed.

## Review Thread Final Alignment Status - 2026-05-11

Aligned

The unresolved non-outdated PR #1130 review threads are fixed in code or tests. Owner/local mode still gates plugin auto-update, shared or unknown/loading relay targets stay non-mutating, older/default disabled behavior remains compatible, and bootstrap remains outside user-installed plugin update logic.

## Investigation Fix Verification Findings - 2026-05-11 Server Snapshot And Telemetry Hardening

| Finding | Decision / Branch | Fix Evidence | Status |
| --- | --- | --- | --- |
| Shared-target org members could submit `pluginAutoUpdateEnabled: true` to `PUT /compute-targets/:id/health-check`, and the service stored the enabled mode after only checking target accessibility. | Derive the persisted auto-update snapshot mode from payload intent plus target ownership. Shared accessible targets can still write non-mutating snapshots, but cannot persist enabled mode. | `computeTargetsService.upsertHealthCheckSnapshot()` now stores enabled mode only when `payload.pluginAutoUpdateEnabled === true` and `target.userId === userId`; `service.test.ts` covers shared-target enabled input persisting disabled mode. | Aligned |
| Structured remediation links accepted any `z.url()` scheme, including schemes unsafe for direct anchor rendering. | Persist only HTTPS remediation URLs. Unsafe schemes fail request validation before storage/rendering. | `validators.ts` validates remediation link URLs with an HTTPS-only refinement; `compute-targets.test.ts` covers rejecting `javascript:` remediation links and skipping snapshot persistence. | Aligned |
| Desktop plugin-update telemetry included nested `diagnostics.pluginUpdate.stderrTail`, but the Datadog-bound sanitizer only scrubbed top-level tails. | Apply the same ANSI/credential-line tail sanitizer to nested plugin-update stderr before logging. | `sanitizeDesktopTelemetryDiagnostics()` now copies and sanitizes `pluginUpdate.stderrTail`; observability and API telemetry tests cover nested credential-line removal before metadata logging. | Aligned |

## Investigation Fixes Applied - 2026-05-11

- Added a server-side ownership check to prevent shared-target users from persisting enabled plugin auto-update snapshots.
- Restricted structured remediation link URLs to HTTPS at the API validation boundary.
- Sanitized nested plugin-update `stderrTail` before Desktop telemetry diagnostics are written to Datadog-bound logs.

## Investigation Validation - 2026-05-11

- `pnpm -C apps/api test app/compute-targets/service.test.ts __tests__/api/compute-targets.test.ts __tests__/desktop-telemetry-handler.test.ts`: passed, 3 files / 76 tests.
- `pnpm -C packages/observability exec vitest run '__tests__/telemetry-schema.test.ts'`: passed, 1 file / 65 tests.
- `pnpm -C apps/api typecheck`: passed.
- `pnpm -C packages/observability typecheck`: passed.
- `pnpm lint`: passed with pre-existing warnings outside this fix.
- `git diff --check`: passed.

## Investigation Final Alignment Status - 2026-05-11

Aligned

The server persistence, remediation-link validation, and nested telemetry-sanitization branches now preserve PRD-258 behavior: owner/local mode remains the only mutating plugin-update path, shared targets cannot create enabled snapshots, unsafe stored remediation links are rejected, and Desktop plugin-update diagnostics are sanitized before logging.

## Local QA Investigation Fix Verification Findings - 2026-05-11 Feature Flag Key Mismatch

| Finding | Decision / Branch | Fix Evidence | Status |
| --- | --- | --- | --- |
| Local QA had `plugin-auto-update-enabled` enabled in PostHog, but app health-check paths still did not auto-update an outdated `code` plugin. | The app-side feature flag lookup must use the same production key documented in the PR and enabled in PostHog. Keep query-key mode segments such as `plugin-auto-update` unchanged because those are cache discriminators, not PostHog flag keys. | `PLUGIN_AUTO_UPDATE_FEATURE_FLAG_KEY` centralizes `plugin-auto-update-enabled`; `SystemCheckBootstrap` and `PreLoopSystemCheckProvider` now read that key before allowing owner/local health checks to send `pluginAutoUpdate=1`. | Aligned |
| The mismatch was not covered by tests because mocks returned enabled for any flag key. | Make tests key-specific so a future typo prevents the auto-update mode from enabling. | `system-check-bootstrap.test.tsx` now enables only `PLUGIN_AUTO_UPDATE_FEATURE_FLAG_KEY` and asserts that key is requested; `pre-loop-system-check-provider.test.tsx` now verifies the same key causes `pluginAutoUpdate=1` to be included in the health-check fetch. | Aligned |

## Local QA Investigation Fixes Applied - 2026-05-11

- Replaced the incorrect app-side PostHog flag lookup key `plugin-auto-update` with the documented production key `plugin-auto-update-enabled`.
- Added a shared app constant for the plugin auto-update feature flag key.
- Added key-specific regression coverage for ambient health checks and pre-loop health checks so enabled users send `pluginAutoUpdate=1` only when the production flag key is enabled.

## Local QA Investigation Validation - 2026-05-11

- `pnpm -C apps/app exec vitest run lib/system-check/__tests__/pre-loop-system-check-provider.test.tsx 'app/(authenticated)/components/__tests__/system-check-bootstrap.test.tsx'`: passed, 2 files / 32 tests.
- `pnpm -C apps/app typecheck`: passed.
- `pnpm -C apps/app exec biome check --write lib/system-check/plugin-auto-update.ts lib/system-check/pre-loop-system-check-provider.tsx lib/system-check/__tests__/pre-loop-system-check-provider.test.tsx 'app/(authenticated)/components/system-check-bootstrap.tsx' 'app/(authenticated)/components/__tests__/system-check-bootstrap.test.tsx'`: passed after formatting.
- `git diff --check`: passed.

## Local QA Investigation Final Alignment Status - 2026-05-11

Aligned

The owner/local plugin auto-update request path now honors the documented PostHog flag. A user enabled for `plugin-auto-update-enabled` should have ambient and pre-loop health checks request the Desktop gateway with `pluginAutoUpdate=1`; shared-target ownership gates and disabled/default compatibility remain unchanged.
